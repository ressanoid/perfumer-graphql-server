type Query {
 info: String!
 feed : [Link!]!
 getLink(id: ID!): Link
}
type Link {
    id: ID!
    url : String!
    description: String!
}
type Mutation {
    post(url: String!, description: String!) : Link!
}